version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: backend_api/Dockerfile
    container_name: paperlessboss_api
    restart: always
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
