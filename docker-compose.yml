version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: backend_api/Dockerfile
    container_name: paperlessboss_api
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
